{
  "name": "friday",
  "private": true,
  "version": "0.1.0",
  "description": "FRIDAY - Gemini-based real-time conversational AI assistant",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "api:dev": "pnpm --filter @friday/api start:dev",
    "api:build": "pnpm --filter @friday/api build",
    "api:test": "pnpm --filter @friday/api test",
    "api:test:e2e": "pnpm --filter @friday/api test:e2e",
    "web:dev": "pnpm --filter @friday/web dev",
    "web:build": "pnpm --filter @friday/web build",
    "web:lint": "pnpm --filter @friday/web lint"
  }
}
